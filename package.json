{
  "name": "presence-turn-runtime",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "type": "module",
  "description": "Companion-chat turn engine: classify once, constraint card, strip without inventing, salvage at most once, mood carries, local memory. Browser-only, bring your own key.",
  "keywords": [
    "llm",
    "chat",
    "companion",
    "dialogue",
    "turn-taking",
    "client-side"
  ],
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node scripts/test-runtime.mjs"
  },
  "devDependencies": {
    "vite": "^8.0.12"
  }
}
